# BetterGI macOS Core extraction map

This file is the source-of-truth map for the first macOS extraction. A row is
only marked `complete` when the linked source is used by the macOS composition
and has behaviour coverage. Compiling a substitute API is not completion.

Upstream synchronization baseline: official `babalae/better-genshin-impact`
`main@780f566` (merged into `mac-core-extraction`). The extraction follows the upstream `Recognition.json`
and capture-size asset-cache architecture; macOS does not retain the removed
asset singleton lifecycle.

| Upstream source | Core compile location | macOS composition | State | Acceptance evidence |
| --- | --- | --- | --- | --- |
| `Core/Config/Global.cs`, `ConfigJson.cs` | linked under `Core/Config` | `RuntimeLayout` supplies the runtime root; App packaging generates its `GameTask` tree directly from the canonical upstream task `Assets` selected by `game-task-assets.manifest`, and Swift atomically swaps that generated tree into the runtime root before launching Core | complete | The Swift source tree contains no second `GameTask` copy. The manifest includes every production-reachable asset slice, including `AutoTrackPath` for the linked `TpTask`; Host verification stages from that same manifest and executes the real SIFT teleport chain. Packaging runs the published Core Host against the actual App resource tree: every packaged `Recognition.json` object is built through `RecognitionObjectJsonLoader` at 1920x1080 and 1280x720, and every referenced template must exist and decode through OpenCV. Swift tests retain only atomic replacement and rollback behavior. |
| `Core/Script/PackageDocumentLoader.cs` | linked under `Core/Script` | `ScriptProject` | complete | Host verification evaluates a package import and rejects a path escape. |
| `Core/Script/Project/{Manifest,ScriptProject}.cs` | linked under `Core/Script/Project` | `ScriptProjectCatalog`, `MacScriptProjectHostInitializer` | partial | Manifest, settings, package and module DTOs are read by Core. The real `ExitGameMultipleMode` project executes its Alt+F4 branch through ClearScript and the acknowledged macOS input callback in upstream keyDown/keyUp order. The production initializer exposes every host root, member and canonical global referenced by the five-project real-user fixture; complete execution of its game-dependent projects remains a live capture/input gate rather than a missing EngineExtend surface. |
| `Core/Script/Group/{ScriptGroup,ScriptGroupProject,ScriptGroupConfig}.cs` | linked under `Core/Script/Group`, including the real execution partial | `ScriptGroupCatalog`, `SchedulerCoordinator`; Swift receives Core-produced summaries, setting schemas and typed values, and sends only explicit edit commands without decoding or writing the upstream document | partial | The macOS scheduler exposes four add paths, project context operations, group settings, clear/reverse/path-folder refresh and one-shot next-project start marker. Core atomically owns every mutation, preserves unedited group fields and unknown historical JS setting keys, applies `SettingItem` defaults with the same string/bool/list semantics as WPF, validates select and multi-select options, and computes the manifest HTTP allow-list hash so script updates revoke authorization. The upstream `--startGroups <name...>` argument implies runtime startup and sends one ordered `scheduler.runGroups` request; Core resolves every group, retains one task ID and lifecycle, executes groups with the upstream two-second interval and continuous-group flag, and persists the original `TaskProgress` document instead of sequencing groups in Swift. `scheduler.status` exposes the same Core-owned task ID, group, lifecycle and terminal error as a queryable snapshot, so reconnect and live verification do not infer state from UI logs; Swift synchronizes it after Core startup. Control-file export executes the shared `JsonMerger` for every Pathing project and opens the generated runtime-log directory only after Core returns a real file count. The `script-group-editing`, `scheduler-status` and `runtime-settings` fast suites cover catalog, lifecycle and authenticated RPC contracts without OpenCV/ONNX startup. Log analysis remains explicitly unavailable until its Core business slice is extracted; full real-user group execution remains pending. |
| real `User/ScriptGroup/狗粮+锄地.json` | loaded by `BetterGenshinImpact.RealUser.Verification` through `ScriptGroupCatalog`, `ScriptGroup`, `ScriptProjectCatalog`, `ScriptProject`, `Manifest`, ClearScript V8 and `PackageDocumentLoader` | canonical runtime root `~/Library/Application Support/betterGI-mac/` | partial | The current real User tree resolves all five referenced JavaScript projects, validates each manifest/main/settings document, preserves `jsScriptSettingsObject`, reports saved-files/library declarations, compiles every entry and links the real static module graph through the production loader. The verifier initializes the production macOS ClearScript host and audits all JavaScript files in those projects and packages: 31 referenced host members bind against the actual registered types, all 28 canonical globals exist, and the only requested realtime triggers are the composed AutoPick and AutoSkip implementations. It also parses 1,011 Pathing documents directly from the User tree and requires the complete action surface (`combat_script`, `fight`, `force_tp`, `log_output`, `mining`, `pick_around`, `pyro_collect`, `set_time`, `stop_flying`, `up_down_grab_leaf`) to remain covered by the shared `PathExecutor` and behavior gates. Its actual `ExitGameMultipleMode` project executes from the User tree and the recording platform verifies the exact `MENU`/`F4` key-down/key-up order. Full execution of the other four projects remains a live game capture/input gate; it is no longer blocked by an absent script host member, unknown route action or known uncomposed task call. |
| `Service/ScriptService.cs` | linked into Core; only UI/process effects delegate through `ScriptServicePlatform` | Windows uses `WindowsScriptServicePlatform`; macOS uses `MacScriptServicePlatform` and `SchedulerCoordinator` | partial | Host verification now drives RPC through `RunMulti`, recognizes a synthetic 1920x1080 frame containing the real Paimon asset, enters the real `TaskRunner` and Shell branch, verifies the shell artifact, and observes `running → completed`. Pathing constructs the shared `PathExecutor` and the same Core-owned `PathExecutorAutoSkipSessionFactory` on both platforms; its dedicated policy enables quick skipping, popup closing and last-option selection without consulting realtime AutoSkip user settings. `IAutoSkipRuntimePlatform` now exposes only OCR, audio, input and window capabilities, while application composition passes realtime user configuration explicitly to `AutoSkipTrigger`. Remaining failures must come from explicit task capabilities or real recognition/input state, not a missing executor composition. |
| `GameTask/{TaskRunner,RunnerContext,TaskProgress}.cs` | linked scheduler lifecycle state; combat recognition remains in the same `RunnerContext` partial type and will join its task slice | Windows uses `WindowsTaskRunnerPlatform`; macOS uses activation, notification and release-all callbacks; the macOS platform requests unexpected-exception and lock-failure propagation so RPC can publish a truthful terminal state while Windows retains the upstream swallow-and-notify behavior | partial | Core behavior tests execute the real lock, concurrent rejection, cancellation-finally cleanup, exception notification and continuous-group `NormalEndException` propagation. RPC pause/resume uses the same `RunnerContext.IsSuspend` toggled by upstream Windows UI. Host verification executes a real Shell scheduler group, rejects its final platform activation after the command runs, and requires `scheduler.event` to transition from `running` to `failed` rather than `completed`. A second group runs while the real TaskSemaphore is occupied and must emit `failed` without creating its Shell marker. |
| `Core/Script/EngineExtend.cs` and host objects | canonical global registration moved into linked `GlobalMethod`; `ScriptHostServices`, `Log`, `LimitedFile`, `Http`, `ServerTime`, `StrategyFile`, `CustomHostFunctions`, `Notification` and the upstream `BvPage`/`BvLocator`/`BvImage` types are linked | `MacGlobalMethodRuntime`, `MacScriptHostServices`, `MacScriptProjectHostInitializer`, `MacBvRuntimePlatform`, Swift callback adapter | partial | Windows EngineExtend and macOS consume one canonical global-function name list. Real ClearScript, packages, settings, file/HTTP permissions, input ACK, capture ring and notifications are composed. `BvLocator.WithRoi(Func)` obtains capture dimensions from the shared runtime platform; Windows retains the original Fischless simulators while macOS `BvPage` keyboard/mouse proxies dispatch acknowledged semantic input. Host verification executes a standard-module `await genshin.returnMainUi()` through the original `Genshin` and `ReturnMainUiTask`, then constructs all three Bv host types through production ClearScript, matches the real Paimon template from the shared capture ring, and verifies key down/up/press, text entry, relative mouse movement, three button clicks and wheel input in exact order. `genshin.width`, `height` and `screenDpiScale` are read through acknowledged macOS `window.metrics` callbacks rather than duplicated Swift state. The same production initializer executes `genshin.getPositionFromMapWithMatchingMethod` against a hash-checked release 0.62.0 `MapBack_3` fixture: the frame crosses the real BGRA capture ring, the shared mini-map preprocessor normalizes it to BGR, upstream TemplateMatch publishes an acknowledged position DTO, and the returned world coordinate matches the fixture. A standard-module `genshin.switchParty("Team")` call then executes the original `SwitchPartyTask` current-team success branch; the verifier installs only the exact source-locked Paddle V5 OCR artifacts and preheat fixture, recognizes the real party-view template and OCR text after a BGRA ring round-trip, and requires the production metrics/capture/window-activation callback sequence. Upstream `genshin.SwitchCharacter` is also linked as the original attributed state machine: Core owns its filtering, avatar-grid ONNX recognition and slot transition policy, while `IGenshinRuntimePlatform` injects logger, screen metrics, ONNX and OCR resources. `await genshin.tp(-1642.421,2163.664,"Teyvat",true)` now executes the original `TpTask`: an official hash-checked Teyvat big-map crop crosses the BGRA ring, upstream SIFT recognizes its center and rectangle, the shared Region graph sends acknowledged target and real `GoTeleport` button clicks, and a real Paimon template completes the teleport wait. `genshin.tpToStatueOfTheSeven()` additionally restores the upstream runtime-only `GiTpPosition` from the persisted Newtonsoft-compatible document, selects the configured real statue from `tp.json`, performs the outer map check and the same full teleport chain, then executes the configured restore wait with its final activation ACK. Its `ShouldMove` branch uses a coherent release-map minimap track and dynamically aligned camera ring to execute the original `PathExecutor.MoveTo`, acknowledged position publication, forward key-state query/down/up sequence, final Drop action, and restore activation/wait. A separate ClearScript scenario enables only `IsReviveInNearestStatueOfTheSeven`, recognizes a real release big-map center through SIFT, selects source-locked `tp.json` Goddess ID 293 on Seirai Island, verifies the center target and teleport-button click coordinates, and completes the branch-mandated MoveTo/Drop chain. `genshin.ClaimBattlePassRewards()` now constructs the original shared task on macOS; Core verification executes its no-reward branch through main-UI recognition, semantic F4 input, both upstream tab clicks, OCR scans and final main-UI return. `genshin.GoToCraftingBench(country)` now constructs the original task, loads the four canonical upstream routes, obtains PathExecutor metrics and activation through macOS callbacks, and completes only after the shared talk-UI recognizer sees the real disabled-dialogue template; OneDragon selection and Paddle OCR are supplied by platform composition without moving selection or crafting semantics out of C#. `genshin.CraftMaterial(...)` now constructs the original task; material-type resolution, BvPage OCR, GridScreen traversal, ItemV2 matching, slider correction, submit flow and reward fallback remain in shared C#, while input, scale, logging, draw clearing and OCR cross existing composition boundaries. Host verification proves ClearScript reaches the upstream non-positive-quantity validation rather than `CapabilityUnavailable`; full successful in-game crafting remains a real-environment gate. `genshin.chooseTalkOption` now receives its logger, capture metrics and AutoSkip semantic-input platform from `IGenshinRuntimePlatform` composition instead of resolving task-level process globals; the original talk-UI check, option-icon/OCR ordering, delayed second recognition, orange filtering and Region click remain unchanged. Both `genshin.setTime` overloads likewise enter the original `SetTimeTask` through the composed runtime while retaining argument validation, dial geometry, skip-animation behavior and upstream exception semantics. Other genshin task breadth still requires explicit composition. |
| `Core/Script/Dependence/Dispatcher.cs` | the upstream class, timer/cancellation methods, task-name switch, JS config parsing, food-effect rules and inventory request normalization compile in Core; platform-free request records isolate task construction | Windows adapter executes the original task classes and reads the existing ViewModel/TaskContext settings; macOS executes every currently cataloged independent task through the same linked C# implementations | partial | Host verification calls lowercase `dispatcher.addTimer` and `getLinkedCancellationToken` through ClearScript, verifies clear-before-add order, and proves an uncomposed task cannot return success. AutoCook keeps the upstream UI/template checks, perfect-cook color peak state machine, thresholds and delay semantics; macOS injects the persisted Core config, real asset scale and logger, while Space input crosses the shared focus-safe `TaskControlPlatform`. AutoWood retains the upstream task loop, recognition descriptors, wood-count policy and limits; macOS composition loads the Core-owned config while its platform adapter only supplies a `caffeinate` sleep-prevention lease and the explicitly unavailable Windows-only Bilibili login session. CountInventoryItem retains the upstream parameter validation, weapon-ore pre-scroll, single/multiple Grid traversal, selectable gridIcon/ItemV2 recognition, Paddle count OCR, not-found return values and dynamic multi-item result; macOS only injects OCR, logger and structured overlay output. Both Core and Windows solutions build; the real dog-food group now has its required `dispatcher` host object. |
| Core-owned independent tasks | `SoloTaskCoordinator` exposes a truthful catalog and executes normalized requests through the shared dispatcher platform | `solo.list/start/stop/status` RPC; Swift renders only the Core DTO and does not maintain a parallel runnable-task list | partial | AutoGeniusInvokation, AutoFishing, AutoWood, AutoFight, AutoMusicGame, AutoAlbum, AutoCook, AutoRedeemCode, AutoArtifactSalvage, AutoDomain, AutoBoss, AutoLeyLineOutcrop and AutoStygianOnslaught are available and execute normalized requests through the original C# tasks. The shared AutoEat task, used by scripts and AutoDomain rather than exposed as an upstream independent-task card, retains the upstream low-HP nutrition-bag loop, configured check/eat intervals, food inventory Grid traversal, gridIcon ONNX classification, Paddle OCR count parsing, confirmation flow and return-code semantics; macOS injects only the Core-owned config snapshot, live system scale, OCR, logger, semantic input and structured overlay boundary. AutoFishing now builds an explicit shared `AutoFishingTaskParam` from the Core-owned config and page state, so timeout, throw timeout, time policy and key-frame screenshot settings reach the next solo run identically on Windows/macOS; the runtime platform receives atomic config snapshots and the Windows-only Torch DLL field is truthfully read-only on macOS. AutoDomain retains the upstream teleport/entry/combat/tree-search/resin/reward/salvage state machine, BgiTree YOLO, Paddle OCR, localized text rules and 16:9 guard. Its constructor now receives the Core-owned config snapshot, pick key and `IAutoDomainRuntimePlatform`; macOS supplies only SystemInfo, OCR/ONNX, notification, AutoEat registration and structured overlay presentation, while all movement uses the shared focus-safe semantic input boundary. Host verification proves its solo start carries the resolved real combat-strategy path into `DispatcherDomainTaskRequest` and stop cancels the same Core task; Core verification covers the upstream 16:9 acceptance/rejection rule and the locked artifact gate creates the real BgiTree ONNX session. AutoBoss preserves the upstream boss validation, route selection, party switch, TXT/JSON combat, resin parsing and supplement policy, death retry, reward navigation/recognition, relocation and 16:9 guard. It receives Core-owned AutoBoss/AutoFight snapshots, OCR/SystemInfo/notification capabilities and an explicit `IAutoBossPathExecutorFactory`; the task no longer reads PathExecutor, AutoSkip or ScriptGroup service locators while running, and its dedicated pathing config still disables party switching and AutoFight without discarding platform pathing defaults. Host verification proves `solo.start` resolves the configured strategy into `DispatcherBossTaskRequest` and `solo.stop` cancels that same Core task; Core verification covers parameter copying, 2560x1440 acceptance, non-16:9 rejection and real factory/config behavior. AutoArtifactSalvage preserves the upstream 1-4 star quick selection, set filter Grid traversal, grid-icon ONNX classification, Paddle OCR artifact parsing, recognition-failure policy and ClearScript five-star filter. macOS injects the Core-owned config, real OCR service and live asset scale; inventory actions and Escape use the shared focus-safe task-control boundary, while labeled recognition rectangles cross the structured overlay boundary. Core verification runs the real artifact Grid/status fixture, locked Paddle models, gridIcon ONNX/prototypes and V8 filter. AutoFight directly constructs the shared `AutoFightHandler`, preserving its Core-owned strategy resolution, party-config override and TXT/JSON factory selection. Host verification proves each start reaches the correct dispatcher request and stop cancels the same Core task; AutoWood additionally preserves the upstream solo defaults of unlimited rounds (`0`, normalized by `WoodTaskParam`) and a 2000 daily cap. Core verification drives AutoCook with the real cook-icon template and a deterministic stable-peak/drop frame sequence, then requires exactly one semantic Space press. Its AutoWood gate runs `AutoWoodTask.Start` against the real `TheBoonOfTheElderTree` recognition asset, requires exactly one `QuickUseGadget` action, and verifies capture, overlay cleanup, login refresh, and balanced sleep-prevention acquisition/release. The AutoMusicGame gate executes the upstream six-lane 5ms blue-channel hold state machine, darkens only the A lane, requires one key-down/key-up pair, and verifies cancellation releases every pressed input. Windows samples the selected game window DC; macOS samples short-lived cached ScreenCaptureKit frames and sends keys through the focus-safe task-control boundary. The shared game-screenshot task now owns timestamped PNG naming, UID masking and the `log/screenshot` output contract on both platforms; the macOS hotkey reads the current shared-memory frame rather than duplicating capture or image business in Swift. The macOS system-info scale now matches upstream semantics across 1280x720, 1920x1080 and 2560x1440. The existing full PathExecutor behavior gate executes the same AutoFight handler through strategy parsing, semantic macro input and combat-end detection. AutoStygianOnslaught preserves the upstream event navigation, battlefield selection, party switch, TXT/JSON combat, resin-priority reward loop and optional artifact salvage; macOS injects SystemInfo, Paddle OCR, pick-key configuration, notification and the shared salvage task, while its two common walking jobs use the focus-safe TaskControl and overlay boundaries. Other independent tasks remain visibly unavailable until their complete shared task dependency slices are composed. |
| KeyMouse, Pathing, Shell entries | The upstream KeyMouse parser, DPI/rectangle adaptation, event timing and ordering now compile in Core; Pathing model, JSON/control-file merge, `FarmingSession`, shared `TaskControl`, `CameraRotateTask`, `Navigation`, `TrapEscaper`, `PathExecutorSuspend`, the complete upstream mini-map orientation algorithm, `NavigationInstance`, `MapManager`, every upstream scene-map implementation, SIFT feature matching, template matching, embedded `combat_script` parser, the original `CombatCommand.Execution` dispatcher, upstream `SkillBoostHelper`, and the upstream `ShellTask` contract are linked; every active hurry-on input crosses `TaskControlPlatform` and its OCR crosses `ImageRegionOcrPlatform`; game-window validation, current-route publication and expedition-country configuration cross `PathExecutorPlatform`; only Navigation's UI position notification crosses `NavigationPlatform`, while all visual localization and hurry-on decisions remain in C# Core; remaining action handlers are pending | macOS KeyMouse and `GIActions` dispatch use acknowledged semantic RPC through Swift's safety gate; pressed-action state is read from real `CGEventSource` keyboard/mouse state; game dimensions come from `window.metrics`; current route and matched position DTOs are emitted to Swift as presentation-only callbacks; Core owns the expedition-country value initialized through `core.initialize`; recognition and geometry assets are resolved from `Recognition.json` and capture-size caches; `MacShellTaskPlatform` executes `/bin/zsh -lc`; combat command key names are validated at the platform composition boundary | partial | Core verification installs the real 0.62.0 `MapBack_3` descriptor/color/gray artifacts through the hash-checked source-lock pipeline, synthesizes nine ground-truth minimap frames from that release imagery along the tracked `雷音权现前往` route, and drives the exact upstream `Navigation.GetPosition` TemplateMatch call used by `PathExecutor`. All nine positions resolve within 2.05-5.74 image pixels, distance shrinks to the real waypoint then grows after passing it, target bearings remain within 8 degrees, every position reaches the presentation callback, and a black frame is rejected. A second coherent release-imagery track executes the original `PathExecutor.MoveTo`: camera orientation, repeated localization, platform key-state reads, one forward key-down, arrival, and final key-up all run through shared C# and the acknowledged task-control boundary, with no replacement movement loop. The full `PathExecutor.Pathing` gate uses the original configured-team metadata, `ReturnMainUiTask`, `CombatScenes.InitializeTeam`, anomaly handling and a published three-waypoint `PathingTask`; its first point executes a real-user `log_output` action through the upstream logger exactly once, it consumes the release-imagery track, executes `UseGadgetHandler`, then loads a parser-readable TXT strategy from the canonical runtime `User/AutoFight` directory and traverses `ActionFactory` → `AutoFightHandler` → `CombatTaskFactoryProvider` → `AutoFightTask`. The fight emits the strategy's semantic macro input, exits through the shared combat-end detector, increments `SuccessFight`, completes with `SuccessEnd`, and releases forward input after each point and in `finally`. Host verification additionally executes a one-waypoint `force_tp` route through `PathExecutor.Pathing`: the locked two-player screenshot initializes the real combat scene, the route publishes its metadata, opens the map, uses the waypoint coordinate rather than a nearby teleport point, completes the shared SIFT/button/main-UI teleport chain, seeds `NavigationInstance` with the converted image coordinate, and releases forward/right-button input in `finally`. The same suite also constructs the real SIFT/template maps, proves coordinate round-trip, executes an embedded combat script in original order, and verifies suspend/resume. Host verification covers KeyMouse ordering, shared hold-action focus/key-up ordering, real key-state query, 1920x1080 window validation data, Core-owned pathing config/current-route DTOs, OpenCV-coordinate callbacks and a real zsh command. Pinned screenshot gates now prove both real AvatarSide YOLO classification and the upstream OCR fallback: the latter forces classification failure for both sidebar avatars, then recognizes `阿蕾奇诺` and `钟离` from the unchanged game screenshot through locked Paddle V5 and `ImageRegion.Find(OcrThis)`. Other action handlers remain unproved and unsupported remaining execution fails explicitly. |
| `GameTask/AutoPathing/Handler/CombatScriptHandler.cs`, `MiningHandler.cs`, `ElementalCollectHandler.cs`, `StopFlyingHandler.cs`, `UpDownGrabLeafHandler.cs` and `PickAroundHandler.cs` | linked verbatim into Core with shared combat-scene contracts | both Windows and macOS providers call the shared `RunnerContext.GetCombatScenes`; macOS therefore runs `ReturnMainUiTask`, capture, AvatarSide YOLO/OCR fallback and `CombatScenes.InitializeTeam` rather than returning a synthetic scene | partial | Core verification invokes the real combat-script handler and proves `BeforeTask` plus the upstream `keydown` → `wait` → `keyup` loop. Seven SHA-256-pinned screenshots from the repository's fixed `BetterGI.UnitTest.Assets` submodule execute the original visual `CombatScenes.InitializeTeam` path against the source-locked `BgiAvatarSide` model on Apple Silicon, covering host/guest multiplayer layouts and exact recognized avatar names. Host verification forces both classifications to fail on the pinned two-player screenshot and proves the original Paddle OCR fallback reconstructs the same two maintained avatar names. The real mining handler then obtains its cached configured team through `RunnerContext`, parses the upstream built-in mining scripts, selects Zhongli, and drives the real `Avatar.UseSkill(hold)` semantic input after UI/team/skill captures. The real `pyro_collect` route action resolves `ElementalCollectHandler` through `ActionFactory`, obtains a configured real `CombatScenes` team, identifies Yanfei as already active from the upstream fixed index rectangles, and preserves the upstream `Attack(100)` semantic normal attack without a handler or Avatar substitute. The real action factory also resolves the route's empty-parameter `stop_flying` before-handler, emits the plunge attack and exits only after the upstream `SpaceKey`/`XKey` motion recognizer classifies the captured frame as non-flying. The four-leaf-sigil before-handler preserves its two-frame white-point debounce, sends the semantic interaction, resets the camera, and recognizes Fly through the real upstream `key_space.png` template without an unnecessary Jump. It then executes the route's empty-parameter `pick_around` handler for its full default circle, preserving the calculated backward/left movement sequence, nine middle-button camera resets and final key release. Production composition has no empty scene or capability fallback; the other action handlers remain pending. |
| `GameTask/AutoFight/Model/Avatar.cs` input boundary | raw combat-script key dispatch and movement now cross shared semantic platforms; visual/combat behavior remains in the upstream class | Windows adapter retains `MappingKey`, VK and XButton behavior; macOS adapter uses the acknowledged Swift `input.dispatch` channel | partial | Core and Host input gates remain green, Windows WPF builds with the original mapping behavior, and `Avatar.cs` no longer imports Vanara or a WPF ViewModel. The remaining Avatar/CombatScenes visual dependency closure is still pending. |
| `GameTask/AutoFight/{AutoFightTask,AutoFightJsonTask,AutoFightSeek,AutoFightEndDetector}.cs` combat-end detection | the original TXT, JSON and seek flows remain linked in Core; only their identical fixed-pixel white/yellow decision is centralized in shared C# | capture and `OpenPartySetupScreen`/`Drop` semantic input use `TaskControlPlatform`; no Swift or Rust combat-end fallback exists | complete | Core verification constructs the original TXT and JSON task classes from real parser-readable strategy files, sends finished and non-finished 1920x1080 frames through `CaptureToRectArea`, and verifies the exact `OpenPartySetupScreen` → capture → `Drop` sequence plus the finish-only second `OpenPartySetupScreen`. Both paths retain the upstream `(50,790)` progress-bar and `(50,768)` white-tile coordinates and BGR thresholds; Seek calls the same detector without merging its retry, rotation or movement control flow. |
| `TaskControlPlatform`, `AutoFightAssets`, combat helpers | the complete mouse button/scroll/action surface, capture-size `AutoFightAssets`, `ESkillCdTracker`, `ImageDifferenceDetector`, `StringUtils` and `RegexHelper` compile in Core | macOS mouse/action calls use acknowledged `input.dispatch`; asset geometry is selected from each real capture size instead of a process-global initialized instance | partial | `Avatar.cs` has no active direct `Simulation` or WPF draw calls; the new Sandrone tracking path publishes rectangles through `OverlayDrawPlatform`. Windows retains the same SendInput and overlay behavior through its adapters. Core verification proves same-size asset reuse and different-size cache separation. |
| `GameTask/AutoFishing` task and behavior layer | the upstream `AutoFishingTask`, `AutoFishingTaskParam`, `AutoFishingTrigger`, `SetTimeTask`, `ReturnMainUiTask`, assets, models, Blackboard, full `Behaviours`, BehaviourTree base/extensions, OpenCV recognizer, RodNet/TorchSharp and GridIcon preprocessing compile directly in Core | shared acknowledged input/capture/focus boundary; runtime-root config persistence; embedded upstream task resources; Windows and macOS share the exact UID-cover transform; macOS executes the same SetTime/return-to-main-UI recognition loop and saves behavior screenshots under runtime `log/screenshot` | complete | Core behavior verification executes the real `MoveViewpointDown` state transition and exact input order, deterministic fish-bar recognition, upstream localization, dial geometry, coordinate routing and UID masking. No alternate fishing, time-adjustment or screenshot implementation exists. |
| `GameTask/LogParse/ExecutionRecord*` | linked under `GameTask/LogParse` | runtime-root log storage and `ScriptHostServices` server time | complete | Core verification executes the original static storage against the runtime root, checks the physical `log/ExecutionRecords/yyyyMMdd.json` document and upstream Newtonsoft field names, proves same-GUID replacement, reloads two daily files in reverse date order, and covers successful/failed records plus positive and negative cases for all three skip policies. The same public `IsSkipTask` path verifies the server-time 04:00 boundary and both `StartTime`/`EndTime` gap reference points. |
| OCR/ONNX/OpenCV pipeline | linked under `Core/Recognition`; the canonical manifest/source-lock covers all 19 registry task/OCR ONNX models, the ItemV2 material model and prototype CSV, 11 model sidecars/fixtures and the two upstream Teyvat big-map SIFT files required by `TpTask` | portable CPU ONNX runtime plus lazy real Paddle OCR; production `core.initialize` verifies or installs every locked artifact before trigger assets initialize, retaining the verified 0.62.0 archive under `Cache/Downloads`; the in-process SharpCompress installer reads the official solid 7z without Homebrew and verifies archive/member size plus SHA-256 | partial | Core verification installs all 34 locked files from the real 0.62.0 portable archive, creates 19/19 registry ONNX Runtime 1.21.0 sessions plus the ItemV2 session, validates its `embedding` output and required CSV columns, and still decodes the real Paddle OCR fixture as `Hello`. Host verification proves the production initialization boundary invokes artifact provisioning exactly once. `ImageRegion` OCR/OcrMatch/ColorRangeAndOcr use the composed real service on macOS and process teardown disposes `OrtEnv`. Task-level composition remains pending; artifact installation does not. |
| `GameTaskManager`, regions, draw output | linked shared manager/regions/overlay command boundary | macOS window metrics, semantic input and applied `overlay.command` callbacks; all template loads use the canonical runtime `GameTask` tree; task adapters defer `ISystemInfo` until Swift has attached the callback channel | partial | All Core `Shim/` files are deleted. Host verification invokes the production asset initializer after callback attach, loads real templates through `GameTaskManager.LoadAssetImage`, ACKs the initialization overlay clear, and requires the exact eight-trigger set: GameLoading, AutoPick, QuickTeleport, AutoSkip, AutoFish, AutoEat, MapMask and SkillCd. Core-owned `trigger.settings.get/save` exposes active settings without moving configuration semantics into Swift. Saving atomically updates `User/config.json`, updates the same live runtime config object and reinitializes the affected shared trigger/assets; `trigger.setEnabled` also persists the upstream `enabled` property. Asset initialization no longer implicitly starts capture: Core-owned `runtime.status/start/stop` RPCs drive a restartable dispatcher lifecycle. Each trigger invocation remains isolated so one exception cannot stop later triggers on the same frame. Overlay drawing accepts the upstream derived `Region` graph and preserves its converter chain to game-capture coordinates. Swift applies named rectangle, label, text, remove and clear commands to a typed store before ACK, then maps capture physical pixels to the HUD's Quartz-point dimensions at runtime; recognition labels preserve recognized/unrecognized colors and SkillCd uses the same Core text positions. Invalid payloads are rejected rather than logged as success. MapMask now links the upstream MiHoYo, HoYoLab and Kongying point services and restores provider-specific selection and hidden state from `User/MapMask`. Its real-time trigger settings expose only the upstream mini-map-mask switch; provider, language, label-tree validation and selection persistence use dedicated `mapMask.settings/catalog/selection` RPCs from the interactive big-map HUD picker. The shared trigger contract supports multiple stable UI categories, so MapMask retains the upstream `Unknown` main-UI path while continuing `BigMap` updates after the dispatcher's 30-second category window on both Windows and macOS. The shared capture path applies upstream `DeriveTo1080P()` semantics to 2560x1440 frames while preserving the Region converter back to physical pixels. Core caches the validated label catalog per provider/language and merges independent trigger UI fields instead of dropping viewport results. Matching the upstream `PointsCanvas` lifecycle, selection changes publish the raw point collection once while pan/zoom publishes only the latest world-map viewport; viewport callbacks never serialize or resend the point collection. A single Swift Canvas performs the same display-only viewport transform, and its dedicated observable store limits high-frequency invalidation to the HUD layer instead of the entire application UI tree. Full-map points and the circular `MapAssets.MimiMapRect1080P` area share this data flow while the main overlay remains mouse-transparent. AutoFight, AutoFishing, TpTask and Bv composition before server start no longer query an unattached callback; a published self-contained Host creates its 0600 socket before any callback exists. |
| `GameTask/QuickSereniteaPot/QuickSereniteaPotTask.cs` and auxiliary macros | linked shared task and macro implementations under `GameTask`; Core owns hotkey descriptors and press/release or one-shot operation lifecycles | Swift reports physical edges only; macOS adapters provide current capture geometry, foreground-safe semantic input, bounded waits, logging and overlay cleanup | partial | The original quick-Serenitea-Pot flow now owns inventory opening, bag/pot template retries, white-confirm recognition, main/big-map recovery, OCR interaction validation and final enter/leave sequence in shared C#. Windows retains its original TaskContext, SendInput and Toast behavior through `WindowsQuickSereniteaPotRuntimePlatform`; macOS packages the canonical `QuickSereniteaPot` assets and executes through the acknowledged capture/input boundary. A Core one-shot coordinator deduplicates repeated presses and cancels the action on runtime stop or game exit. Fast verification locks the upstream runtime/focus guards, final F/click order and one-shot cancellation. Other upstream auxiliary actions remain partial until their original task slices are composed. |

AutoSkip settings now round-trip through `trigger.settings.get/save`: Core owns
the upstream dialogue, option-priority, process-audio VAD, submit, popup,
daily-reward, expedition and hangout fields, atomically persists them, and
mutates the same configuration instance held by the running trigger. The
Windows-only background activation and PiP controls are intentionally absent.
SkillCd uses the same Core-owned path for role fallback rules, trigger policy,
position, spacing, scale and colors; Swift only renders the returned style and
text commands, and does not couple the timer to recognition-debug visibility.

Pathing host composition note: the production `pathingScript` receives
`IScriptGroupExecutionServices` in its constructor and creates executors through
that instance. Its `run`, `runFile`, and `runFileFromUser` paths no longer resolve
process-global PathExecutor services during script execution.

Independent-task settings follow the same ownership rule as execution. `solo.settings.get/save` exposes typed upstream settings for AutoGeniusInvokation, AutoFishing, AutoCook, AutoWood, AutoMusicGame, AutoAlbum, AutoBoss, AutoFight, AutoDomain, AutoArtifactSalvage, AutoLeyLineOutcrop and AutoStygianOnslaught and atomically persists their config models to `User/config.json`. AutoFishing exposes the two upstream timeout ranges, Core-supplied `FishingTimePolicy` options and page-only key-frame screenshot state; each save preserves hidden `Enabled`, auto-throw and Torch-path fields, updates the live runtime snapshot and feeds the next normalized dispatcher request. The Windows-only Torch DLL path remains visible but unavailable on macOS rather than pretending to take effect. AutoWood's upstream page-only round count and daily limit remain Core session state and feed the actual dispatcher request; AutoBoss options come from the shared boss catalog and the live `User/AutoFight` tree, with its model normalization preserved. AutoFight exposes only fields bound by the upstream independent-task page, publishes each atomic save to the live macOS runtime snapshot, and preserves hidden fields such as `teamNames`, `skipModel`, elite-drop policy and color diagnostics. AutoDomain exposes the upstream strategy, party, domain, resin, salvage, movement, recovery and retry controls; one save updates `autoDomainConfig`, `autoFightConfig.strategyName` and `autoArtifactSalvageConfig.maxArtifactStar` while preserving non-presented fields such as `sundaySelectedValue` and `resinPriorityList`. AutoArtifactSalvage exposes the upstream five-star JavaScript, set filter, quick-salvage star ceiling, inspection limit and recognition-failure policy, with the policy labels supplied by Core and legacy non-presented fields preserved. AutoLeyLineOutcrop links the original shared task, routes all input through `TaskControl`, injects OCR, AutoFight, PathExecutor, notification and overlay capabilities, packages its canonical 639-file asset slice, and exposes every field actually bound by the upstream WPF expander. Its Core-owned save preserves hidden synthesis and fight-finish settings, and the next solo start passes that same config through `DispatcherLeyLineTaskRequest`; live combat, reward and resin-flow completion remains a real-game acceptance step. AutoStygianOnslaught exposes exactly the upstream strategy, battlefield, party, resin mode/count and artifact-salvage controls; its save preserves the hidden `ResinPriorityList` and only updates `MaxArtifactStar` in the shared salvage config. `solo.list.settingsAvailable` remains false for every task whose complete upstream settings slice is not yet composed, so Swift does not display a nonfunctional disclosure control.

AutoGeniusInvokation is now part of the composed independent-task set. Core links
the complete upstream parser, card definitions, dice/HP/status recognition and
duel state machine. Each run creates one explicit control session shared by
`Duel` and `Character`; the shared domain no longer resolves `TaskContext`,
Win32 input or a process-global control singleton. Core owns the selected
`User/AutoGeniusInvokation/*.txt` strategy and the 0-5000 ms delay snapshot,
while macOS injects only live metrics, Paddle OCR, focus-safe capture/input,
logging, diagnostics and notifications. Swift exposes exactly the two controls
bound by the upstream WPF expander and never parses strategy text.

AutoAlbum now reuses the same shared `AutoMusicGameTask`, recognition assets and
focus-safe semantic input boundary while preserving the upstream album-page
validation, OCR distinction from the all-songs page, difficulty/reward traversal,
concurrent song-end detection and full-album loop. Core owns the shared
`autoMusicGameConfig` snapshot for both task entries; Swift reuses the typed
settings DTO and does not create a second album configuration model. macOS
propagates task failures to `solo.status`, while Windows retains the upstream
notification-and-return behavior.

AutoRedeemCode now links the original `UseRedemptionCodeTask`: ReturnMainUI,
settings/account navigation, OCR locators, per-code paste/confirm handling,
success/failure classification and final UI restoration remain in shared C#.
The Core catalog declares a typed multiline startup-input contract; Swift only
renders the generic editor and sends its text unchanged, while Core trims and
validates lines before creating `DispatcherRedeemCodeTaskRequest`. macOS
clipboard writes and cleanup use authenticated platform callbacks, preserving
the upstream in-game Paste-button flow without moving code parsing into Swift.
The upstream automatic clipboard-listener setting remains unavailable until its
Core workflow is composed; no inert toggle is shown.

## First-step completion gate

| Requirement | State | Reproducible evidence |
| --- | --- | --- |
| Apple Silicon Core Host | complete | `mac-core.yml` requires an arm64 runner, builds the Core solution and publishes a self-contained `osx-arm64` Host. |
| Bundled Helper viability | complete | The frontend packaging script creates a standard `APPL` bundle with a regular activation policy, embeds the self-contained publish tree at `Contents/Resources/BetterGICore`, signs every nested Mach-O, seals the outer App, and starts the Host from the final signed App path with `--dependency-smoke`, loading OpenCV, ClearScript V8 and the self-contained .NET runtime. Local packages automatically use an available stable Apple Development identity so TCC grants survive rebuilds; the hardened Core Host alone receives the JIT, executable-memory and library-validation entitlements required by CoreCLR, and dependency smoke runs again after signing. The publish tree is deliberately not placed directly in `Contents/Helpers`, whose nested-code rules reject loose managed DLLs during strict outer signing. |
| Windows WPF regression | complete | `.github/workflows/wpf-build.yml` is a hard-failure `windows-latest` gate for pull requests and `mac-core-extraction` pushes, including shared Core and Host composition paths; the full Windows solution also cross-builds locally with zero errors. |
| C# script and scheduler authority | complete | `scheduler.run/runGroups/pause/resume/stop`, ScriptGroup persistence, `TaskProgress`, ClearScript, Dispatcher, Pathing, KeyMouse and Shell are Core-owned; the static gate rejects the retired Swift scheduler, manifest and ScriptGroup implementations. Swift parses the upstream `--startGroups` spelling but sends the complete ordered group list to one Core RPC instead of implementing the sequence. |
| Swift RPC and input boundary | complete | Core callbacks require acknowledgements; Swift applies runtime-lifecycle, foreground, synthetic-window, emergency-stop and rate-limit checks before dispatching `CGEvent`. The home, header, command-menu and menu-bar controls all use one RPC-backed runtime toggle whose label changes between Start and Stop; Swift updates its independent runtime lifecycle only after Core ACK. `--start-runtime` invokes this same start path only after Core is ready and a real game window is selected. Normal launches use real input like the Windows application; development and diagnostics can explicitly select non-dispatching behavior with `--dry-run`. The three development toggles are not exposed in production UI. Runtime-trigger input consults the runtime lifecycle and foreground guard rather than scheduler state. A platform dispatch exception is returned to Core as an input rejection rather than an ACK, and Swift tests exercise that callback failure path. Core preserves the callback connection after this application-level rejection so the scheduler can emit its terminal `failed` event; transport and protocol failures still detach the channel. Swift consumes that terminal event by clearing the active task, entering the error state and displaying the Core error; a late `scheduler.run` response cannot overwrite a terminal event. HUD and piped Core output share one asynchronous runtime log writer with daily files, per-file and aggregate byte caps, count and age retention, while manual export remains a separate bounded snapshot. |
| No production business fallback | complete | The static gate rejects Core shims, skipped verification, Swift OCR/YOLO/map/task translations, Swift ONNX, fake state, mock actions and empty runnable controls. Uncomposed task branches return `CapabilityUnavailable`. |
| Real User contract | complete | The local verifier loads all five projects in `狗粮+锄地`, compiles their module graphs through the production loader, audits 31 referenced host members and 28 canonical globals, requires the exact behavior-verified eight-member `genshin` and three-member `dispatcher` surfaces, restricts timers to AutoPick/AutoSkip, parses 1,011 real Pathing documents and rejects any action outside the ten-member shared action surface, and executes the real `ExitGameMultipleMode` input sequence. |
| Live game execution | partial | A real `GenshinImpact.exe` window is available through Wine and the development App has reached Core `Ready`. The source-locked 0.62.0 archive was verified from the production cache, provisioning progress was visible through the Swift log pipe, the shared trigger dispatcher produced measured capture FPS, and the Core-owned catalog returned all five enabled projects in `狗粮+锄地`. The HUD was also verified to follow the selected window by stable `CGWindowID` after the game window moved. `--startGroups 千星` and `--startGroups 狗粮+锄地` now provide reproducible upstream-compatible launch paths through the same Core scheduler. WeeklyThousandStarRealm, AutoHoeingOneDragon, AAA-Artifacts-Bulk-Supply and AbundantOre still require full completion against the real game window; normal launches use the foreground-guarded input path, while `--dry-run` remains available for non-dispatching diagnostics. |

The CI gate intentionally does not replace the last row with a synthetic fixture: hosted runners do not contain the user's `User` tree or a running game. A real window, Core startup, provisioning, capture and catalog discovery are now recorded, but the overall first-step status remains **partial** until the four game-dependent projects complete through explicitly authorized real input.

## Non-negotiable production boundary

### macOS client geometry policy

- Wine's title bar is excluded from both ScreenCaptureKit/Quartz frames and the HUD; `window.metrics` reports the client area's physical pixels rather than Quartz points.
- HUD compass and UID masking use the upstream 1920x1080 coordinate constants. At 2560x1440 the observed `1280x752 pt @2x` Wine frame resolves to a `1280x720 pt` / `2560x1440 px` game client.
- Window movement only repositions the HUD. A client pixel-size change is debounced and calls the Core-owned `runtime.refreshGeometry` lifecycle, which stops capture, reloads the real upstream assets/triggers, preserves trigger enabled states, and resumes only when the runtime was previously running.
- `runtime.start`, `runtime.stop`, and geometry refresh share one mutation lock, so a stop request cannot be undone by a late resolution refresh. Host verification covers running and stopped refresh behavior, and shared asset loading closes its source stream before returning the decoded OpenCV `Mat`.

### macOS input focus policy

- Production input uses `pauseUntilFocused`: Core waits while Genshin is unfocused and resumes only after the user returns to it.
- macOS adapters do not call `window.activate`, use AX to steal focus, or use `CGEvent.postToPid` for background game input.
- After focus returns, Core sends `releaseAll` before the pending operation; Swift `InputSafetyGate` still verifies the selected game PID before every real dispatch.
- Focus waits honor task cancellation and never silently fall back to global input.
- `runtime.stop` is a Core-owned stop transaction: it cancels and awaits any active scheduler and independent task before stopping the trigger dispatcher. A scheduler cancelled while waiting for focus emits `cancelled`, not `failed`, and independent-task cancellation flows into the same foreground wait.
- During the Swift `.stopping` state, the input gate permits only a foreground-verified `releaseAll`; every key, mouse, scroll and text action remains blocked. The packaged App reproduced `scheduler running -> scheduler cancelled -> runtime stopped`, and a subsequent runtime start exposed no active scheduler task.
- A transient capture callback failure is isolated to the current dispatcher iteration. Core retries after a bounded backoff, logs only the first and every twentieth consecutive failure, and records recovery instead of leaving Swift in a false `Running` state with a dead trigger loop.
- Independent-task descriptors derive their expandable-settings capability from the Core settings catalog. Input-only tasks such as redemption-code execution no longer advertise a settings panel that the upstream task does not own.
- Core verification has explicit cumulative execution boundaries: `contracts`, `artifacts`, `models`, `recognition`, and `all`. The measured Debug baseline was about 16 seconds to build versus 105 seconds to execute the former monolithic verifier; local development can now stop before locked-archive installation, ONNX session creation, OCR inference, or map localization, while CI continues to run `--suite all`.

`scheduler.run`, `scheduler.pause`, `scheduler.resume`, and `scheduler.stop`
are owned by Core and call the shared `ScriptService.RunMulti` chain. Swift may
only display catalog/state and issue these RPCs; it must not call the existing
Swift/Rust scheduler. An individual uncomposed task branch must terminate with
structured `CapabilityUnavailable`, never success. The configured-team
two-waypoint Pathing and `use_gadget` action branch is now composed; its
unverified visual-team and other action-handler branches must not be reported
as complete.

Historical Swift translations of BetterGI tasks, script-repository semantics,
template/OCR/YOLO recognition, and big-/mini-map localization are removed from
the App source tree. Swift no longer links ONNX Runtime; model loading and
deterministic OCR are mandatory C# Core verification gates.
The realtime-feature page and HUD are likewise populated from `trigger.list`;
Swift owns only presentation metadata and sends the returned Core trigger name
unchanged to `trigger.setEnabled`.

## Verification tiers

`scripts/verify-core-fast.sh <suite>` is the development loop for contracts
that have been moved into ordinary suite classes. It builds Core and Host once,
builds the small verifier without rebuilding dependencies, and runs only the
requested suite. The `trigger-settings` suite validates the real Core-owned
AutoPick, AutoSkip and SkillCd configuration, persistence and live-update paths. The `solo-settings` suite
validates Core-owned independent-task round trips and typed dispatcher requests,
including AutoGeniusInvokation, AutoLeyLineOutcrop and AutoStygianOnslaught,
without starting ONNX,
OpenCV or the complete RPC host.

The existing Core, Host and RealUser verifiers remain the full submission/CI
gates. Further isolation follows actual dependency boundaries (`Fast`,
`Recognition`, `Models`) and moves existing scenarios when implementation work
touches them. Empty verifier projects, duplicated assertions and broad test-only
refactors are not accepted as progress.

## Required real-world compatibility fixture

The local runtime contains `User/ScriptGroup/狗粮+锄地.json`. It is the first
non-synthetic scheduler fixture. It references enabled JavaScript projects
under `User/JsScript`: `WeeklyThousandStarRealm`, `AutoHoeingOneDragon`,
`AAA-Artifacts-Bulk-Supply`, `AbundantOre`, and `ExitGameMultipleMode`. It
exercises populated Pathing/AutoPick/AutoFight/Shell configuration, per-project
settings, and manifest `saved_files` globs. Scheduler compatibility is not
proven until Core loads the full fixture and every referenced manifest/settings
declaration without Swift-side interpretation.

## Composition rule

The shared C# sources remain the semantic owner. Platform code may provide only
runtime-root storage, capture, semantic input dispatch, window metrics,
dialogs, notifications, and draw-command presentation. `#if BGI_PLATFORM_MAC`
may isolate a WPF/Win32 host implementation, never change a script, task, or
scheduler decision.
